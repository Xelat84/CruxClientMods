<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>

	<groupId>org.gotti.wurmunlimited</groupId>
	<artifactId>client-modlauncher-parent</artifactId>
	<version>0.17-SNAPSHOT</version>
	<packaging>pom</packaging>

	<name>client modlauncher parent</name>

	<modules>
		<module>modules/modlauncher</module>
		<module>modules/patcher</module>
		<module>modules/mods/connectionfix</module>
		<module>modules/mods/custommap</module>
		<module>modules/mods/serverpacks</module>
		<module>modules/wurmclientkit</module>
		<module>modules/crux</module>
		<module>modules/dist</module>
	</modules>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<maven.compiler.source>1.8</maven.compiler.source>
		<maven.compiler.target>1.8</maven.compiler.target>
	</properties>

	<!--
	  In-project Maven repository (committed under repo/). Holds the two signature-only WU client stubs
	  (client / common-client 3721782) and the prebuilt modloader-shared 0.18 jar, so the whole reactor
	  builds with zero external hosts and zero secrets. maven.multiModuleProjectDirectory is the reactor
	  root and stays constant for every child module (unlike project.basedir). Everything else (javassist,
	  assembly/shade plugins, ...) still resolves from Maven Central.
	-->
	<repositories>
		<repository>
			<id>project-local</id>
			<name>In-project client stubs + modloader-shared</name>
			<url>file:///${maven.multiModuleProjectDirectory}/repo</url>
			<releases>
				<enabled>true</enabled>
			</releases>
			<snapshots>
				<enabled>false</enabled>
			</snapshots>
		</repository>
	</repositories>

	<scm>
		<connection>scm:git:https://github.com/Xelat84/CruxClientMods.git</connection>
		<developerConnection>scm:git:git@github.com:Xelat84/CruxClientMods.git</developerConnection>
		<url>https://github.com/Xelat84/CruxClientMods</url>
		<tag>HEAD</tag>
	</scm>

	<build>
		<plugins>
			<plugin>
				<groupId>ru.concerteza.buildnumber</groupId>
				<artifactId>maven-jgit-buildnumber-plugin</artifactId>
				<version>1.2.9</version>
				<inherited>true</inherited>
				<executions>
					<execution>
						<id>git-buildnumber</id>
						<goals>
							<goal>extract-buildnumber</goal>
						</goals>
						<phase>prepare-package</phase>
						<configuration>
							<javaScriptBuildnumberCallback>
								(tag ? tag : branch) + "-" + shortRevision
							</javaScriptBuildnumberCallback>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>2.4</version>
				<inherited>true</inherited>
				<configuration>
					<archive>
						<manifestEntries>
							<Implementation-Version>${git.buildnumber}</Implementation-Version>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<artifactId>maven-resources-plugin</artifactId>
				<version>3.0.2</version>
				<inherited>true</inherited>
				<executions>
					<execution>
						<id>copy-properties</id>
						<phase>process-resources</phase>
						<goals>
							<goal>copy-resources</goal>
						</goals>
						<configuration>
							<outputDirectory>${project.build.outputDirectory}</outputDirectory>
							<overwrite>true</overwrite>
							<resources>
								<resource>
									<directory>${project.basedir}/src/dist</directory>
									<targetPath>META-INF/org.gotti.wurmunlimited.modloader</targetPath>
									<includes>
										<include>${project.name}.properties</include>
									</includes>
									<filtering>true</filtering>
								</resource>
							</resources>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>
